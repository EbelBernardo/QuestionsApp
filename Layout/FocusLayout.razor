@*
    ══════════════════════════════════════════════════════════════════════════
    Layout/FocusLayout.razor — NOVO ARQUIVO
    ──────────────────────────────────────────────────────────────────────────
    Layout do modo de estudo (Pages/Cards.razor).

    É intencionalmente vazio: no modo foco não existe menu, painel nem
    cabeçalho. A página inteira é o card. A única saída é o botão de fechar,
    que a própria página desenha.

    Existe como layout (em vez de a página simplesmente não ter menu) para
    que o Blazor não aplique o MainLayout por padrão.
    ══════════════════════════════════════════════════════════════════════════
*@
@inherits LayoutComponentBase

@Body
